# Framework
fastapi==0.116.1
uvicorn[standard]==0.35.0
streamlit
# 0.4.4+ aceita um schema JSON no parametro format, o que restringe a
# decodificacao ao formato esperado na classificacao de triagem.
ollama>=0.4.4

# Configuração
python-dotenv==1.1.1
requests
python-multipart

# Validação
pydantic==2.11.7
pandas
pydantic-settings==2.10.1

# Speech to Text
faster-whisper==1.1.1
ctranslate2>=4.5.0
av>=12.0.0

#ChromaDB
chromadb # Banco Vetorial
sentence-transformers # Modelo que transforma textos em embeddings
transformers # Tokenizer explícito usado pelo chunking consciente de tokens

# Extração de documentos
pymupdf==1.28.2 # Extrator principal com blocos e coordenadas (AGPL-3.0/comercial)
pypdf # Fallback simples quando a extração estruturada falhar
fonttools # Decodificação completa das fontes CFF encontradas em papers
