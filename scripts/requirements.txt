# Dependencias dos scripts de dados e avaliacao.
# Rodam fora do Docker, na maquina do desenvolvedor:
#   python -m venv .venv && pip install -r scripts/requirements.txt

pandas
requests
pytest

# Só para regerar os áudios do benchmark de voz (generate_voice_benchmark.py).
# Medir o WER com run_voice_benchmark.py não precisa dele — os MP3 já estão
# versionados em scripts/voice_benchmark/audio/.
# edge-tts
